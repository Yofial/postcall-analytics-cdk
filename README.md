# Post-Call Analytics – CDK (JavaScript)

```bash
npm install

# deploy with existing bucket:
cdk deploy --all -c existingBucket=<YOUR_EXISTING_BUCKET>

# destroy
cdk destroy --all -c existingBucket=<YOUR_EXISTING_BUCKET>

